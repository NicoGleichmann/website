import type { Localize } from ".pnpm/date-fns@4.1.0/node_modules/date-fns/locale/types";
import type {
  FirstWeekContainsDateOptions,
  LocalizedOptions,
  WeekOptions,
} from ".pnpm/date-fns@4.1.0/node_modules/date-fns/types";
type Formatter = (
  date: Date,
  token: string,
  localize: Localize,
  options: Required<
    LocalizedOptions<"options"> & WeekOptions & FirstWeekContainsDateOptions
  >,
) => string;
export declare const formatters: {
  [token: string]: Formatter;
};
export {};
