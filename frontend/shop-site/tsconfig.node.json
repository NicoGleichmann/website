/*This is the tsconfig.node.json file for the shop-site frontend project*/
{
  "compilerOptions": {
    "composite": true, // Muss auf true stehen, wenn es von tsconfig.json referenziert wird
    "skipLibCheck": true,// Überprüft keine Typen in den Node.js-Bibliotheken
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "jsx": "react-jsx",
    "types": ["node"] // Wichtig, damit __dirname erkannt wird
  },
  "include": ["vite.config.ts"] // Nur die Vite-Konfigurationsdatei
}